# Optional — enables the LLM enrichment path in the agents and SQLite
# checkpointing. Without these the graph runs fully on deterministic agent
# logic with an in-memory checkpointer.
#   pip install -r requirements.txt -r requirements-ai.txt
langchain-core>=1.6,<2
langchain-openai>=1.6,<2
langchain-google-genai>=4.3,<5
langgraph-checkpoint-sqlite>=2,<3
langsmith>=0.11,<1
